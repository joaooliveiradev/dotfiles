---
description: Pause work — write a session handoff so another session can resume cleanly
---

Invoke the `craft` skill via the Skill tool (skill: "craft"), then execute the **Pause work / create handoff** trigger from `SKILL.md`. Load `shared/.claude/skills/craft/references/handoff.md` and produce `HANDOFF.md`.
