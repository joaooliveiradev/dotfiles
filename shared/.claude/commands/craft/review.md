---
description: Review phase — gate check, validate WHEN/THEN criteria, audit code, UAT, write review.md
---

Invoke the `craft` skill via the Skill tool (skill: "craft"), then execute the **Review** trigger from `SKILL.md`. Load `shared/.claude/skills/craft/references/review.md` and run that workflow end-to-end.

The slug is read from `STATE.md`. Update requirement traceability in `spec.md` as part of the review.
