---
description: Run the Spec phase — Research (grill mode) then Specify (PRD with traceable IDs)
---

Invoke the `craft` skill via the Skill tool (skill: "craft"), then execute the **Start spec** trigger from `SKILL.md`. Load `shared/.claude/skills/craft/references/spec.md` and run that workflow end-to-end.

The slug is read from `STATE.md` — do not ask the user for it.
