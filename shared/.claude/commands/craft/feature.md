---
description: Start a new feature — pick mode (spec/quick), set slug, create branch + folder
---

Invoke the `craft` skill via the Skill tool (skill: "craft"), then execute the **Start feature** trigger from `SKILL.md`. Load `shared/.claude/skills/craft/references/feature.md` and run that workflow end-to-end.
