---
description: Run the Tasks phase — Plan (architecture) then Tasks (atomic breakdown)
---

Invoke the `craft` skill via the Skill tool (skill: "craft"), then execute the **Start tasks** trigger from `SKILL.md`. Load `shared/.claude/skills/craft/workflow/tasks/tasks.md` and run that workflow end-to-end.

The slug is read from `STATE.md` — do not ask the user for it.
