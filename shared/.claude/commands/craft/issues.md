---
description: Run the Issues phase — Plan (architecture) then Tasks (atomic breakdown)
---

Invoke the `craft` skill via the Skill tool (skill: "craft"), then execute the **Start issues** trigger from `SKILL.md`. Load `shared/.claude/skills/craft/references/issues.md` and run that workflow end-to-end.

The slug is read from `STATE.md` — do not ask the user for it.
