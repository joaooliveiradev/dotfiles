---
description: Quick mode — Describe → Pre-check → Implement → Verify → STATE.md (skips spec/plan)
---

Invoke the `craft` skill via the Skill tool (skill: "craft"), then execute the **Start quick mode** trigger from `SKILL.md`. Load `shared/.claude/skills/craft/references/quick-mode.md` and run that workflow end-to-end.

The slug is read from `STATE.md` — do not ask the user for it.
